import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.praca.estoque',
  appName: 'A Praça - Estoque',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
